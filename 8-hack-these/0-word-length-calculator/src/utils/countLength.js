/**
 * Calculates the length of the given word and returns it as a string.
 * @param {string} word - The word whose length needs to be calculated.
 * @returns {string} - The length of the word as a string.
 */

const countLength = (word) => {
    const count = word.length;
    return String(count);
};

export default countLength;
